import clsx from 'clsx'

import React from 'react'

interface Props {
  isBlack: boolean
}

const blackSrc = '/api/media/file/black_ribbon.png'
const whiteSrc = '/api/media/file/white_ribbon.png'

export const Ribbon = (props: Props) => {
  return
}
